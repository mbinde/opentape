# User data (keep structure, ignore contents)
userdata/songs/*
userdata/settings/*
!userdata/songs/.gitkeep
!userdata/settings/.gitkeep
!userdata/settings/.htaccess

# Legacy paths (for old installs)
songs/*.mp3
settings/*.json
settings/*.array
settings/*.php

# Server config
.htaccess
.htpasswd

# General
*.mp3
.DS_Store
daemon.*
